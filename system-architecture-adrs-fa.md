# ADRهای عمومی برای آزمون معماری سیستم

# ۱. قابلیت انتقال‌پذیری (Portability)

## ADR 1 — استفاده از کانتینرها (Containerization)

### کانتکست
سیستم باید بتواند در محیط‌های مختلف مانند توسعه، تست، مرحله‌ی آزمایشی و تولید اجرا شود. همچنین ممکن است در آینده نیاز باشد سیستم از یک زیرساخت یا ارائه‌دهنده‌ی ابری به زیرساخت دیگری منتقل شود. وابستگی مستقیم برنامه به سیستم‌عامل یا محیط اجرا، این انتقال را دشوار می‌کند.

### تصمیم
اجزای اصلی سیستم در قالب کانتینرهایی مانند Docker بسته‌بندی شوند. تنظیمات وابسته به محیط نیز خارج از برنامه و از طریق متغیرهای محیطی یا فایل‌های پیکربندی مدیریت شوند.

### راه‌حل‌های جایگزین
- نصب مستقیم برنامه روی ماشین مجازی
- ایجاد محیط‌های جداگانه بدون استفاده از کانتینر
- استفاده از معماری بدون سرور (Serverless)

### پیامد مثبت
- افزایش قابلیت انتقال سیستم بین محیط‌های مختلف
- یکسان شدن محیط اجرای برنامه
- کاهش خطاهای ناشی از تفاوت محیط‌ها
- ساده‌تر شدن استقرار و مقیاس‌پذیری

### پیامد منفی
- افزایش پیچیدگی زیرساخت
- نیاز به مخزن کانتینر (Container Registry)
- نیاز به ابزارهای مدیریت و هماهنگ‌سازی کانتینرها (Orchestration)


## ADR 2 — کاهش وابستگی به ارائه‌دهنده‌ی زیرساخت

### کانتکست
وابستگی مستقیم برنامه به سرویس‌های اختصاصی یک ارائه‌دهنده‌ی ابری می‌تواند مهاجرت به ارائه‌دهنده‌ی دیگر را دشوار و پرهزینه کند.

### تصمیم
برای سرویس‌های مهم زیرساختی مانند ذخیره‌سازی، پیام‌رسانی و پایگاه داده، تا حد امکان از لایه‌های انتزاعی (Abstraction Layer) یا رابط‌های مشخص استفاده شود تا منطق کسب‌وکار مستقیماً به سرویس اختصاصی یک شرکت وابسته نباشد.

### راه‌حل‌های جایگزین
- استفاده مستقیم از سرویس‌های اختصاصی ارائه‌دهنده‌ی ابری
- طراحی کامل سیستم برای یک ارائه‌دهنده‌ی خاص
- طراحی چندابری (Multi-Cloud) از ابتدای پروژه

### پیامد مثبت
- کاهش قفل‌شدگی به فروشنده (Vendor Lock-in)
- ساده‌تر شدن مهاجرت
- امکان تغییر فناوری با تغییر کمتر در منطق کسب‌وکار
- افزایش قابلیت تست

### پیامد منفی
- افزایش تعداد لایه‌های نرم‌افزاری
- افزایش پیچیدگی کد
- ممکن است نتوان از تمام قابلیت‌های اختصاصی یک ارائه‌دهنده استفاده کرد


# ۲. قابلیت استقرار (Deployability)

## ADR 1 — استفاده از خط لوله‌ی استقرار خودکار (CI/CD)

### کانتکست
استقرار دستی باعث افزایش خطای انسانی، تفاوت بین محیط‌ها و کند شدن انتشار نسخه‌های جدید می‌شود.

### تصمیم
مراحل ساخت، تست و استقرار سیستم از طریق خط لوله‌ی خودکار (CI/CD Pipeline) انجام شود و هر نسخه به صورت یک خروجی نسخه‌دار (Versioned Artifact) تولید شود.

### راه‌حل‌های جایگزین
- استقرار دستی
- استفاده از اسکریپت‌های نیمه‌خودکار
- انتشار نسخه‌های بزرگ در بازه‌های زمانی طولانی

### پیامد مثبت
- کاهش خطای انسانی
- انتشار سریع‌تر نسخه‌ها
- تکرارپذیری فرایند استقرار
- امکان بازگشت راحت‌تر به نسخه‌ی قبلی
- اجرای خودکار تست‌ها قبل از انتشار

### پیامد منفی
- نیاز به طراحی و نگهداری خط لوله
- افزایش پیچیدگی زیرساخت
- خرابی خط لوله می‌تواند انتشار نسخه را متوقف کند


## ADR 2 — استقرار تدریجی

### کانتکست
انتشار نسخه‌ی جدید نباید باعث از دسترس خارج شدن کامل سیستم شود. همچنین باید بتوان مشکلات نسخه‌ی جدید را قبل از تأثیرگذاری بر تمام کاربران شناسایی کرد.

### تصمیم
نسخه‌های جدید به صورت تدریجی و با روش‌هایی مانند استقرار چرخشی (Rolling Deployment) یا استقرار قناری (Canary Deployment) منتشر شوند.

### راه‌حل‌های جایگزین
- توقف نسخه‌ی قبلی و سپس اجرای نسخه‌ی جدید
- استقرار آبی-سبز (Blue-Green Deployment)
- استقرار دستی روی تمام نمونه‌ها

### پیامد مثبت
- کاهش زمان قطعی
- کاهش ریسک انتشار نسخه
- امکان آزمایش نسخه‌ی جدید روی بخشی از ترافیک
- امکان بازگشت سریع‌تر به نسخه‌ی قبلی

### پیامد منفی
- افزایش پیچیدگی فرایند استقرار
- اجرای همزمان چند نسخه برای مدتی کوتاه
- نیاز به سازگاری نسخه‌های مختلف API و پایگاه داده


# ۳. امنیت (Security)

## ADR 1 — استفاده از TLS و mTLS

### کانتکست
اطلاعات حساس ممکن است هنگام انتقال بین کاربر، سرویس‌های داخلی و سرویس‌های خارجی مورد شنود یا تغییر قرار بگیرند.

### تصمیم
تمام ارتباطات خارجی از پروتکل امن HTTPS مبتنی بر TLS استفاده کنند. برای ارتباطات حساس بین سرویس‌های داخلی نیز از TLS دوطرفه (mTLS) استفاده شود.

### راه‌حل‌های جایگزین
- استفاده از HTTP بدون رمزنگاری
- استفاده از TLS فقط بین کاربر و سرور
- اعتماد به شبکه‌ی خصوصی داخلی بدون رمزنگاری

### پیامد مثبت
- رمزنگاری داده‌ها هنگام انتقال
- کاهش خطر شنود اطلاعات
- احراز هویت بهتر سرویس‌ها در mTLS
- افزایش امنیت ارتباطات داخلی و خارجی

### پیامد منفی
- افزایش پیچیدگی مدیریت گواهی‌ها
- نیاز به تعویض دوره‌ای گواهی‌ها
- ایجاد سربار پردازشی اندک


## ADR 2 — احراز هویت متمرکز و اصل کمترین دسترسی

### کانتکست
اگر هر سرویس احراز هویت و کنترل دسترسی را به شکل مستقل پیاده‌سازی کند، احتمال ناسازگاری و آسیب‌پذیری امنیتی افزایش پیدا می‌کند.

### تصمیم
احراز هویت به صورت متمرکز و بر اساس استانداردهایی مانند OAuth 2.0، OpenID Connect یا JWT انجام شود. همچنین دسترسی کاربران و سرویس‌ها بر اساس اصل کمترین سطح دسترسی (Least Privilege) تعیین شود.

### راه‌حل‌های جایگزین
- پیاده‌سازی مستقل احراز هویت در هر سرویس
- استفاده از کلید API ساده
- استفاده از Session متمرکز

### پیامد مثبت
- مدیریت متمرکز هویت
- کاهش تکرار منطق امنیتی
- کنترل بهتر مجوزها
- ساده‌تر شدن ثبت و بررسی فعالیت‌ها (Audit)

### پیامد منفی
- سرویس احراز هویت به یک وابستگی مهم تبدیل می‌شود
- تنظیمات امنیتی پیچیده‌تر می‌شوند
- نیاز به مدیریت انقضا و باطل کردن توکن‌ها


# ۴. مقیاس‌پذیری (Scalability)

## ADR 1 — مقیاس‌پذیری افقی (Horizontal Scaling)

### کانتکست
بار سیستم ممکن است در طول زمان افزایش پیدا کند یا در بعضی ساعات به صورت ناگهانی زیاد شود و یک نمونه از برنامه دیگر توان پاسخ‌گویی به تمام درخواست‌ها را نداشته باشد.

### تصمیم
برنامه تا حد امکان بدون حالت (Stateless) طراحی شود و چند نمونه از آن به صورت همزمان اجرا شوند. درخواست‌ها نیز از طریق متعادل‌کننده‌ی بار (Load Balancer) بین آن‌ها توزیع شوند. در Kubernetes می‌توان از مقیاس‌گذاری خودکار افقی (HPA) استفاده کرد.

### راه‌حل‌های جایگزین
- مقیاس‌پذیری عمودی (Vertical Scaling)
- افزایش دستی تعداد نمونه‌ها
- استفاده از یک سرور بسیار قدرتمند

### پیامد مثبت
- افزایش ظرفیت پاسخ‌گویی
- امکان مقیاس‌گذاری خودکار
- افزایش دسترس‌پذیری
- توزیع بار بین چند نمونه

### پیامد منفی
- افزایش پیچیدگی سیستم توزیع‌شده
- نیاز به خارج کردن Session و State از برنامه
- نیاز به متعادل‌کننده‌ی بار
- دشوارتر شدن اشکال‌زدایی


## ADR 2 — تقسیم افقی پایگاه داده (Sharding)

### کانتکست
حجم داده یا تعداد عملیات خواندن و نوشتن به حدی رسیده است که یک نمونه‌ی پایگاه داده دیگر پاسخ‌گوی نیاز سیستم نیست.

### تصمیم
داده‌ها بر اساس یک کلید مناسب تقسیم‌بندی (Shard Key) مانند شناسه‌ی کاربر، شناسه‌ی مشتری یا منطقه، بین چند پایگاه داده تقسیم شوند.

### راه‌حل‌های جایگزین
- افزایش منابع پایگاه داده
- استفاده از نسخه‌های فقط‌خواندنی (Read Replica)
- پارتیشن‌بندی داخلی پایگاه داده
- استفاده از پایگاه داده‌ی توزیع‌شده

### پیامد مثبت
- افزایش ظرفیت نوشتن
- توزیع بار بین چند پایگاه داده
- امکان نگهداری حجم بسیار بیشتر داده
- امکان مقیاس‌گذاری مستقل هر بخش

### پیامد منفی
- انجام Query و Join بین بخش‌ها دشوار می‌شود
- انتخاب کلید تقسیم‌بندی بسیار مهم است
- توزیع مجدد داده‌ها دشوار می‌شود
- پیچیدگی عملیاتی افزایش پیدا می‌کند


# ۵. کارایی (Performance)

## ADR 1 — استفاده از Cache

### کانتکست
برخی داده‌ها به دفعات زیاد خوانده می‌شوند و مراجعه‌ی مکرر به پایگاه داده یا سرویس‌های خارجی باعث افزایش زمان پاسخ و بار سیستم می‌شود.

### تصمیم
برای داده‌هایی که زیاد خوانده می‌شوند، از حافظه‌ی نهان (Cache) مانند Redis استفاده شود. همچنین برای داده‌ها زمان انقضا (TTL) تعریف شود.

### راه‌حل‌های جایگزین
- خواندن مستقیم از پایگاه داده در هر درخواست
- استفاده از حافظه‌ی داخلی هر نمونه‌ی برنامه
- بهینه‌سازی Query بدون استفاده از Cache

### پیامد مثبت
- کاهش زمان پاسخ
- کاهش بار پایگاه داده
- افزایش ظرفیت پردازش درخواست‌ها
- کاهش فراخوانی سرویس‌های خارجی

### پیامد منفی
- مدیریت به‌روزرسانی و حذف Cache دشوار است
- امکان مشاهده‌ی داده‌ی قدیمی وجود دارد
- نیاز به زیرساخت اضافی
- خرابی Cache باید به درستی مدیریت شود


## ADR 2 — پردازش غیرهمزمان (Asynchronous Processing)

### کانتکست
برخی عملیات مانند ارسال ایمیل، اعلان، تولید گزارش یا پردازش‌های سنگین لازم نیست قبل از پاسخ دادن به کاربر تکمیل شوند.

### تصمیم
این عملیات از طریق صف پیام یا سامانه‌ی رویداد (Message Queue / Event Broker) به صورت غیرهمزمان انجام شوند.

### راه‌حل‌های جایگزین
- اجرای تمام عملیات به صورت همزمان
- اجرای Thread پس‌زمینه در همان برنامه
- پردازش دسته‌ای دوره‌ای (Batch Processing)

### پیامد مثبت
- کاهش زمان پاسخ به کاربر
- افزایش توان پردازش سیستم
- کاهش وابستگی مستقیم بین اجزا
- امکان مقیاس‌گذاری مستقل مصرف‌کننده‌ها

### پیامد منفی
- ایجاد سازگاری نهایی (Eventual Consistency)
- پیچیده‌تر شدن اشکال‌زدایی
- نیاز به زیرساخت صف یا Message Broker
- نیاز به مدیریت پیام‌های تکراری و شکست عملیات


# ۶. جداسازی خرابی (Fault Isolation)

## ADR 1 — الگوی Bulkhead

### کانتکست
خرابی یا کندی یک سرویس وابسته نباید تمام Threadها، Connectionها یا منابع سیستم را مصرف کرده و باعث اختلال در سایر قسمت‌ها شود.

### تصمیم
برای سرویس‌ها یا عملیات مختلف، منابع جداگانه مانند Thread Pool، Connection Pool یا Worker Pool در نظر گرفته شود.

### راه‌حل‌های جایگزین
- استفاده از یک Resource Pool مشترک
- افزایش تعداد Threadها
- افزایش تعداد نمونه‌ها بدون جداسازی منابع

### پیامد مثبت
- محدود شدن دامنه‌ی خرابی
- جلوگیری از خرابی زنجیره‌ای (Cascading Failure)
- ادامه‌ی فعالیت بخش‌های سالم سیستم

### پیامد منفی
- ممکن است استفاده از منابع بهینه نباشد
- تنظیمات سیستم پیچیده‌تر می‌شوند
- تعیین اندازه‌ی مناسب Poolها نیازمند مانیتورینگ است


## ADR 2 — استفاده از Circuit Breaker

### کانتکست
اگر یک سرویس خارجی یا داخلی از دسترس خارج شود، ارسال مداوم درخواست به آن می‌تواند منابع سیستم را مصرف کرده و باعث خرابی زنجیره‌ای شود.

### تصمیم
برای ارتباط با سرویس‌های راه دور از الگوی قطع‌کننده‌ی مدار (Circuit Breaker) همراه با Timeout و Retry محدود استفاده شود.

### راه‌حل‌های جایگزین
- تلاش مجدد نامحدود
- استفاده فقط از Timeout
- شکست مستقیم تمام درخواست‌ها بدون بررسی وضعیت سرویس

### پیامد مثبت
- جلوگیری از خرابی زنجیره‌ای
- کاهش فشار روی سرویس خراب
- شکست سریع درخواست‌ها (Fail Fast)
- امکان بازگشت خودکار پس از بهبود سرویس

### پیامد منفی
- نیاز به مدیریت وضعیت Circuit Breaker
- تنظیم Threshold مناسب دشوار است
- ممکن است سرویس بازیابی شده باشد ولی مدار هنوز باز باقی بماند


# ۷. سازگاری داده (Consistency)

## ADR 1 — استفاده از سازگاری قوی (Strong Consistency)

### کانتکست
برای برخی اطلاعات مانند پرداخت، موجودی حساب، مالکیت یا موجودی کالا، مشاهده‌ی داده‌ی قدیمی یا متناقض قابل قبول نیست.

### تصمیم
برای اطلاعات حیاتی از سازگاری قوی و Transaction استفاده شود تا عملیات مرتبط به صورت اتمی (Atomic) انجام شوند.

### راه‌حل‌های جایگزین
- استفاده از سازگاری نهایی
- تکثیر غیرهمزمان داده
- حل تعارض پس از ایجاد آن

### پیامد مثبت
- کاهش احتمال ناسازگاری داده
- مشاهده‌ی آخرین مقدار معتبر
- مناسب برای عملیات مالی و حساس

### پیامد منفی
- افزایش زمان پاسخ
- مقیاس‌پذیری کمتر نسبت به سازگاری نهایی
- در شرایط قطع ارتباط شبکه ممکن است دسترس‌پذیری کاهش پیدا کند


## ADR 2 — استفاده از سازگاری نهایی (Eventual Consistency)

### کانتکست
در سیستم‌های توزیع‌شده، نگه داشتن یک Transaction همزمان بین چند سرویس باعث افزایش وابستگی و کاهش دسترس‌پذیری می‌شود.

### تصمیم
برای عملیات توزیع‌شده از سازگاری نهایی استفاده شود و برای فرایندهای چندمرحله‌ای از الگوهایی مانند Saga و عملیات جبرانی (Compensating Transaction) استفاده شود.

### راه‌حل‌های جایگزین
- تراکنش توزیع‌شده و Two-Phase Commit
- قرار دادن تمام عملیات در یک سرویس و یک پایگاه داده
- استفاده از سازگاری قوی بین تمام سرویس‌ها

### پیامد مثبت
- کاهش وابستگی بین سرویس‌ها
- مقیاس‌پذیری بیشتر
- دسترس‌پذیری بهتر
- جلوگیری از قفل طولانی منابع

### پیامد منفی
- اطلاعات ممکن است برای مدتی ناسازگار باشند
- طراحی عملیات جبرانی دشوار است
- دنبال کردن و اشکال‌زدایی فرایندها پیچیده‌تر می‌شود


# ۸. قابلیت بازیابی (Recoverability)

## ADR 1 — پشتیبان‌گیری و بازیابی تا یک نقطه‌ی زمانی

### کانتکست
خرابی پایگاه داده، حذف اشتباه اطلاعات یا خراب شدن داده‌ها ممکن است باعث از دست رفتن اطلاعات مهم شود.

### تصمیم
پشتیبان‌گیری (Backup) به صورت خودکار و دوره‌ای انجام شود و برای پایگاه داده‌های حیاتی از قابلیت بازیابی تا یک نقطه‌ی زمانی مشخص (Point-in-Time Recovery) استفاده شود. فرایند بازیابی نیز باید به صورت دوره‌ای آزمایش شود.

### راه‌حل‌های جایگزین
- پشتیبان‌گیری دستی
- استفاده صرف از Replication
- گرفتن Snapshotهای نامنظم

### پیامد مثبت
- امکان بازیابی اطلاعات
- کاهش میزان از دست رفتن داده
- امکان بازگشت به زمان مشخص
- افزایش آمادگی برای بازیابی پس از بحران

### پیامد منفی
- افزایش هزینه‌ی ذخیره‌سازی
- نیاز به سیاست نگهداری Backup
- فرایند بازیابی ممکن است زمان‌بر باشد
- Backup بدون آزمایش بازیابی قابل اعتماد نیست


## ADR 2 — استقرار در چند Availability Zone

### کانتکست
خرابی یک سرور، منطقه‌ی دسترس‌پذیری یا مرکز داده نباید باعث از دسترس خارج شدن طولانی سیستم شود.

### تصمیم
اجزای حیاتی در چند منطقه‌ی دسترس‌پذیری (Availability Zone) اجرا شوند و برای سیستم‌های بسیار حساس، برنامه‌ی بازیابی بحران (Disaster Recovery) در یک Region دیگر نیز در نظر گرفته شود.

### راه‌حل‌های جایگزین
- استقرار در یک Availability Zone
- بازیابی صرفاً از Backup
- معماری Active-Active بین چند Region

### پیامد مثبت
- کاهش زمان قطعی
- افزایش قابلیت بازیابی
- مقاومت بیشتر در برابر خرابی زیرساخت
- بهبود زمان بازیابی (RTO)

### پیامد منفی
- افزایش هزینه‌ی زیرساخت
- افزایش پیچیدگی Replication
- احتمال ایجاد چالش در سازگاری داده بین Regionها


# ۹. قابلیت تغییر (Modifiability)

## ADR 1 — معماری ماژولار

### کانتکست
تغییر یک قابلیت نباید باعث تغییر گسترده در قسمت‌های نامرتبط سیستم شود. وابستگی زیاد بین اجزا هزینه‌ی توسعه و نگهداری را افزایش می‌دهد.

### تصمیم
سیستم بر اساس اصول انسجام بالا (High Cohesion) و وابستگی پایین (Low Coupling) به ماژول‌ها یا سرویس‌های دارای مسئولیت مشخص تقسیم شود.

### راه‌حل‌های جایگزین
- معماری یکپارچه بدون مرزبندی مشخص بین ماژول‌ها
- استفاده کامل از Microservices
- تقسیم سیستم فقط بر اساس لایه‌های فنی

### پیامد مثبت
- تغییر ساده‌تر قابلیت‌ها
- افزایش قابلیت تست
- کاهش تأثیر تغییرات روی سایر بخش‌ها
- امکان توسعه‌ی مستقل‌تر اجزا

### پیامد منفی
- طراحی مرز مناسب بین اجزا دشوار است
- ممکن است لایه‌های اضافی ایجاد شوند
- برای سیستم‌های کوچک ممکن است باعث پیچیدگی غیرضروری شود


## ADR 2 — قرارداد API و نسخه‌بندی

### کانتکست
تغییر API یک سرویس ممکن است برنامه‌های مشتری یا سایر سرویس‌های وابسته را خراب کند.

### تصمیم
APIها دارای قرارداد مشخص باشند و تغییرات ناسازگار از طریق نسخه‌بندی API یا حفظ سازگاری با نسخه‌های قبلی (Backward Compatibility) مدیریت شوند.

### راه‌حل‌های جایگزین
- تغییر مستقیم API بدون نسخه‌بندی
- به‌روزرسانی همزمان تمام مصرف‌کننده‌ها
- استفاده کامل از ارتباط رویدادمحور

### پیامد مثبت
- امکان تغییر مستقل‌تر سرویس‌ها
- کاهش تغییرات شکننده (Breaking Change)
- امکان مهاجرت تدریجی مصرف‌کننده‌ها
- افزایش قابلیت تغییر سیستم

### پیامد منفی
- نیاز به نگهداری چند نسخه
- افزایش تعداد تست‌ها
- نسخه‌های قدیمی باید در نهایت مدیریت و حذف شوند


# ۱۰. قابلیت اطمینان (Reliability)

## ADR 1 — افزونگی و بازیابی خودکار

### کانتکست
خرابی یک نمونه از برنامه نباید باعث از دسترس خارج شدن کل سیستم شود.

### تصمیم
برای اجزای حیاتی چند نمونه اجرا شود، ترافیک از طریق متعادل‌کننده‌ی بار توزیع شود و از بررسی سلامت (Health Check) و بازیابی خودکار (Self-Healing) برای جایگزینی نمونه‌های خراب استفاده شود.

### راه‌حل‌های جایگزین
- اجرای یک نمونه
- راه‌اندازی مجدد دستی
- افزایش منابع یک سرور

### پیامد مثبت
- افزایش دسترس‌پذیری
- تحمل خرابی یک نمونه
- بازیابی خودکار
- امکان نگهداری سیستم بدون قطعی کامل

### پیامد منفی
- افزایش هزینه‌ی زیرساخت
- نیاز به متعادل‌کننده‌ی بار
- مدیریت State پیچیده‌تر می‌شود


## ADR 2 — Idempotency و تلاش مجدد کنترل‌شده

### کانتکست
در سیستم‌های توزیع‌شده ممکن است به دلیل Timeout یا خطای شبکه مشخص نباشد که درخواست قبلی موفق شده است یا خیر. ارسال مجدد ساده‌ی درخواست می‌تواند باعث اجرای تکراری یک عملیات مانند پرداخت یا ثبت سفارش شود.

### تصمیم
عملیات حیاتی به صورت Idempotent طراحی شوند و تلاش مجدد (Retry) با تعداد محدود و با استفاده از Exponential Backoff و Jitter انجام شود.

### راه‌حل‌های جایگزین
- عدم انجام Retry
- Retry فوری و نامحدود
- Retry بدون استفاده از Idempotency

### پیامد مثبت
- افزایش قابلیت اطمینان در برابر خرابی‌های موقت
- جلوگیری از اجرای تکراری عملیات
- کاهش فشار روی سرویس خراب
- افزایش احتمال موفقیت پس از خرابی موقت

### پیامد منفی
- نیاز به Idempotency Key و نگهداری وضعیت درخواست
- افزایش پیچیدگی پیاده‌سازی
- Retry می‌تواند زمان پاسخ بعضی درخواست‌ها را افزایش دهد
